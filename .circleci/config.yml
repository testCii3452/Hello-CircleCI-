version: 2
jobs:
  one:
    docker:
      - image: circleci/nodes:4.8.2

    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.
  two:
   docker:
      - image: circleci/nodes:4.8.2

    steps:
      - checkout

      - run:
          name: Print the Current Time
          command: date
          
workflows:
  version: 2
  one_and_two:
    jobs:
      - one
      - two
