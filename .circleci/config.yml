version: 2
jobs:
  one:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
  two:
   docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      
workflows:
  version: 2
  
  my_workflows:
    jobs:
      - one
      - two
